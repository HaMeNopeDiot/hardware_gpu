#import "@preview/codelst:2.0.2": sourcecode

= Приложение 1
#align(center, [*Листинг файла wrapper.h*])
#figure(
  sourcecode(lang: "Cpp")[#raw(read("code/wrapper.h"))]
)

#pagebreak()
= Приложение 2
#align(center, [*Листинг файла wrapper.cpp*])
#figure(
  sourcecode(lang: "Cpp")[#raw(read("code/wrapper.cpp"))]
)

#pagebreak()
= Приложение 3
#align(center, [*Листинг файла lark.g4*])
#figure(
  sourcecode(lang: "PlainText")[#raw(read("code/lark.g4"))]
)

#pagebreak()
= Приложение 4
#align(center, [*Листинг файла load_nir.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/load_nir.py"))]
)

#pagebreak()
= Приложение 5
#align(center, [*Листинг файла instruction.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/instruction.py"))]
)


#pagebreak()
= Приложение 6
#align(center, [*Листинг файла nir_visitor.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/nir_visitor.py"))]
)

#pagebreak()
= Приложение 7
#align(center, [*Листинг файла transpiler.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/transpiler.py"))]
)

#pagebreak()
= Приложение 8
#align(center, [*Листинг файла optimizer.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/optimizer.py"))]
)

#pagebreak()
= Приложение 9
#align(center, [*Листинг файла code_generation.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/basic_block_generation.py"))]
)

#pagebreak()
= Приложение 10
#align(center, [*Листинг файла nir_dump*])
#figure(
  sourcecode(lang: "PlainText")[#raw(read("code/PREPARSED_MESA_SHADER_VERT"))]
)


#pagebreak()
= Приложение 11
#align(center, [*Листинг файла vert_shader.asm*])
#figure(
  sourcecode(lang: "asm")[#raw(read("code/logged.asm"))]
)

#pagebreak()
= Приложение 12
#align(center, [*Листинг файла assembler.py*])
#figure(
  sourcecode(lang: "python")[#raw(read("code/assembler.py"))]
)
